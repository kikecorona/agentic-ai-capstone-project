// Stub — the Quasar app-vite router wrapper that lived here is no
// longer used. main.js now builds the router directly from
// src/router/routes.js. Keeping this file with a stub export so any
// stale import doesn't crash the build; nothing should import it.
export {};
